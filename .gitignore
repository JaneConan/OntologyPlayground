/node_modules
/oh_modules
/local.properties
/.idea
**/build
/.hvigor
.cxx
/.clangd
/.clang-format
/.clang-tidy
**/.test
/.appanalyzer

# HarmonyOS / OpenHarmony signing materials (contain credentials; never commit)
*.p12
*.p7b
*.cer
*.csr
*.key
*.pem
*.jks
*.keystore
harmony-config.json
key.properties

# DevEco auto-signing config writes plaintext keyPassword/storePassword here — keep out of git
/build-profile.json5

# User-supplied AI-generated / image assets staged by the user (not source-of-truth)
material/